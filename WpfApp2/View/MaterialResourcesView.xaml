<UserControl x:Class="Vuzol.View.MaterialResourcesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Vuzol.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <DataGrid Name="employeeDataGrid" 
                  ItemsSource="{Binding MaterialResourcesList}" 
                  SelectedItem="{Binding SelectedMaterialResources}" 
                  SelectedValue="{Binding SelectedMaterialResources}" 
                  IsReadOnly="True" 
                  AutoGenerateColumns="False"
                  Style="{StaticResource ModernDataGridStyle}"
                  ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                  CellStyle="{StaticResource DataGridCellStyle}"
                  RowStyle="{StaticResource DataGridRowStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Width="Auto" Header="Номер" Binding="{Binding Id}"/>
                    <DataGridTextColumn Width="Auto" Header="Опис" Binding="{Binding Description}"/>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,10,0,10">
            <Button Content="Додати" Name="AddBtn" Command="{Binding AddMaterialResourcesCommand}" Style="{StaticResource AddButtonStyle}"/>
            <Button Content="Коригувати" Name="EditBtn" Command="{Binding EditMaterialResourcesCommand}" Style="{StaticResource EditButtonStyle}"/>
            <Button Content="Видалити" Name="DelBtn" Command="{Binding DelMaterialResourcesCommand}" Style="{StaticResource DeleteButtonStyle}"/>
            <Button Content="Повернутись назад" Name="HomeBtn" Command="{Binding HomeCommand}" Style="{StaticResource SecondaryButtonStyle}"/>
        </StackPanel>
    </Grid>
</UserControl>
