<UserControl x:Class="Vuzol.View.AddView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Vuzol.View"
             xmlns:convertor="clr-namespace:Vuzol.Converteres"
             mc:Ignorable="d" 
             d:DesignHeight="1000" Background="#FAFAFA">

    <UserControl.Resources>
        <!-- Конвертер для отримання тексту помилки -->
        <Style x:Key="ErrorToolTipStyle" TargetType="ToolTip">
            <Setter Property="Background" Value="#FFFFE0E0"/>
            <Setter Property="BorderBrush" Value="Red"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#D8000C"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="8"/>
        </Style>

        <!-- Стиль для TextBox з валідацією -->
        <Style x:Key="ValidatedTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource InputTextBoxStyle}">
            <Setter Property="Validation.ErrorTemplate">
                <Setter.Value>
                    <ControlTemplate>
                        <DockPanel>
                            <Border BorderBrush="Red" BorderThickness="2" CornerRadius="3">
                                <AdornedElementPlaceholder/>
                            </Border>
                        </DockPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="ToolTip">
                        <Setter.Value>
                            <ToolTip Style="{StaticResource ErrorToolTipStyle}"
                                     Content="{Binding RelativeSource={RelativeSource Self}, Path=PlacementTarget.(Validation.Errors)[0].ErrorContent}"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль для ComboBox з валідацією -->
        <Style x:Key="ValidatedComboBoxStyle" TargetType="ComboBox" BasedOn="{StaticResource InputComboBoxStyle}">
            <Setter Property="Validation.ErrorTemplate">
                <Setter.Value>
                    <ControlTemplate>
                        <DockPanel>
                            <Border BorderBrush="Red" BorderThickness="2" CornerRadius="3">
                                <AdornedElementPlaceholder/>
                            </Border>
                        </DockPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="ToolTip">
                        <Setter.Value>
                            <ToolTip Style="{StaticResource ErrorToolTipStyle}"
                                     Content="{Binding RelativeSource={RelativeSource Self}, Path=PlacementTarget.(Validation.Errors)[0].ErrorContent}"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Padding="20">
            <StackPanel DataContext="{Binding PropertyAdd}">
                <!-- Osnovni parametri -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource CardTitleStyle}" Text="Основні параметри"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Style="{StaticResource LabelStyle}">
                                    <Run Text="Інвентарний номер"/>
                                    <Run Text="*" Foreground="Red" FontWeight="Bold"/>
                                </TextBlock>
                                <TextBox Style="{StaticResource ValidatedTextBoxStyle}" 
                                         Text="{Binding InventoryNumber, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True, NotifyOnValidationError=True}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Style="{StaticResource LabelStyle}" >
                                    <Run Text="Найменування"/>
                                    <Run Text="*" Foreground="Red"/>
                                </TextBlock>
                                <TextBox Style="{StaticResource InputTextBoxStyle}" Text="{Binding Name, 
                                    UpdateSourceTrigger=PropertyChanged,ValidatesOnNotifyDataErrors=True, 
                                    NotifyOnValidationError=True}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="4">
                                <TextBlock Style="{StaticResource LabelStyle}">
                                    <Run Text="Заводський номер"/>
                                </TextBlock>
                                <TextBox Style="{StaticResource ValidatedTextBoxStyle}" 
                                         Text="{Binding FactoryNumber, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True, NotifyOnValidationError=True}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="6">
                                <TextBlock Style="{StaticResource LabelStyle}" Text="Мітка(коротке найменування)"/>
                                <ComboBox Style="{StaticResource InputComboBoxStyle}" ItemsSource="{Binding PropertyTypes}" 
                                SelectedValue="{Binding SelectedPropertyType, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Dokumenty -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource CardTitleStyle}" Text="Документи"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Style="{StaticResource LabelStyle}" Text="Формуляр"/>
                                <TextBox Style="{StaticResource InputTextBoxStyle}" 
                                         Text="{Binding FormId, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Style="{StaticResource LabelStyle}" Text="Накладна"/>
                                <TextBox Style="{StaticResource InputTextBoxStyle}" Text="{Binding InvoiceId, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="4">
                                <TextBlock Style="{StaticResource LabelStyle}" Text="Матеріальні активи"/>
                                <ComboBox Style="{StaticResource InputComboBoxStyle}" ItemsSource="{Binding MaterialResources}" 
                                SelectedValue="{Binding SelectedMaterialResources, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="6">
                                <TextBlock Style="{StaticResource LabelStyle}" Text="Категорія"/>
                                <ComboBox Style="{StaticResource InputComboBoxStyle}" ItemsSource="{Binding Categories}" 
                                SelectedValue="{Binding SelectedCategory, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>

                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Knugy obliku -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource CardTitleStyle}" Text="Книга обліку"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Style="{StaticResource LabelStyle}" Text="Книга закріплень"/>
                                <TextBox Style="{StaticResource InputTextBoxStyle}" 
                                         Text="{Binding BookId, UpdateSourceTrigger=PropertyChanged, TargetNullValue=0}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Style="{StaticResource LabelStyle}" Text="Сторінка"/>
                                <TextBox Style="{StaticResource InputTextBoxStyle}" 
                                         Text="{Binding BookPage, UpdateSourceTrigger=PropertyChanged, TargetNullValue=0}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="4">
                                <TextBlock Style="{StaticResource LabelStyle}" Text="Книга обліку"/>
                                <TextBox Style="{StaticResource InputTextBoxStyle}" 
                                         Text="{Binding OrderBookId, UpdateSourceTrigger=PropertyChanged, TargetNullValue=0}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="6">
                                <TextBlock Style="{StaticResource LabelStyle}" Text="Сторінка"/>
                                <TextBox Style="{StaticResource InputTextBoxStyle}" 
                                         Text="{Binding OrderBookPage, UpdateSourceTrigger=PropertyChanged, TargetNullValue=0}"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Finansovie dani -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource CardTitleStyle}" Text="Фінансові характеристики"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Style="{StaticResource LabelStyle}" Text="Наказ на експлуатацію"/>
                                <TextBox Style="{StaticResource InputTextBoxStyle}" Text="{Binding OrderId, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Style="{StaticResource LabelStyle}" Text="Дата наказу"/>
                                <TextBox Style="{StaticResource InputTextBoxStyle}" Text="{Binding OrderDate, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="4">
                                <TextBlock Style="{StaticResource LabelStyle}" Text="Кількість"/>
                                <TextBox Style="{StaticResource InputTextBoxStyle}" Text="{Binding Quantity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="6">
                                <TextBlock Style="{StaticResource LabelStyle}" Text="Одиниця"/>
                                <ComboBox Style="{StaticResource InputComboBoxStyle}" ItemsSource="{Binding QuantityTypes}" 
                                SelectedValue="{Binding SelectedQuantityType, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="8">
                                <TextBlock Style="{StaticResource LabelStyle}" Text="Ціна"/>
                                <TextBox Style="{StaticResource InputTextBoxStyle}" Text="{Binding Price, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Vidpovidalnist -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource CardTitleStyle}" Text="Відповідальність і статус"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="3"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="3"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Style="{StaticResource LabelStyle}">
                                    <Run Text="Відповідальний"/>
                                    <Run Text="*" Foreground="Red" FontWeight="Bold"/>
                                </TextBlock>
                                <ComboBox Style="{StaticResource ValidatedComboBoxStyle}" 
                                          ItemsSource="{Binding Employees}" 
                                          SelectedItem="{Binding SelectedEmployee}"
                                          />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Style="{StaticResource LabelStyle}">
                                    <Run Text="Видано в користування"/>
                                </TextBlock>
                                <ComboBox Style="{StaticResource ValidatedComboBoxStyle}" 
                                          ItemsSource="{Binding Employees}" 
                                          SelectedItem="{Binding SelectedEmployeeProvidedForUse}"
                                          />
                            </StackPanel>
                            <StackPanel Grid.Column="4">
                                <TextBlock Style="{StaticResource LabelStyle}" Text="Статус"/>
                                <ComboBox Style="{StaticResource InputComboBoxStyle}" ItemsSource="{Binding PropertyStatuses}" 
                                          SelectedValue="{Binding SelectedPropertyStatus, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </Grid>
                        <TextBlock Style="{StaticResource LabelStyle}" Text="Додаткова інформація" Margin="0,15,0,5"/>
                        <TextBox Style="{StaticResource InputTextBoxStyle}" Height="80" TextWrapping="Wrap" 
                                 Text="{Binding AdditionalInfo}"/>
                    </StackPanel>
                </Border>

                <!-- Komplektnist zalizo -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource CardTitleStyle}" Text="Комплектність - залізо"/>
                        <DataGrid Style="{StaticResource ModernDataGridStyle}" Height="200"
                                  ItemsSource="{Binding HardwareEquipmentList}" 
                                  SelectedItem="{Binding SelectedHardwareEquipment}" 
                                  IsReadOnly="True" AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Номер" Binding="{Binding CountId}" Width="80"/>
                                <DataGridTextColumn Header="Найменування" Binding="{Binding Description}" Width="*"/>
                                <DataGridTextColumn Header="Кількість" Binding="{Binding Quantity}" Width="80"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                            <Button Style="{StaticResource AddButtonStyle}" Content="Додати" Command="{Binding AddHardwareEquipmentCommand}" Width="100"/>
                            <Button Style="{StaticResource EditButtonStyle}" Content="Змінити" Command="{Binding EditHardwareEquipmentCommand}" Width="100"/>
                            <Button Style="{StaticResource DeleteButtonStyle}" Content="Видалити" Command="{Binding DelHardwareEquipmentCommand}" Width="100"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Komplektnist prohramne zabezpechennya -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource CardTitleStyle}" Text="Комплектність - програмне забезпечення"/>
                        <DataGrid Style="{StaticResource ModernDataGridStyle}" Height="200"
                                  ItemsSource="{Binding SoftwareEquipmentList}" 
                                  SelectedItem="{Binding SelectedSoftwareEquipment}" 
                                  IsReadOnly="True" AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Номер" Binding="{Binding CountId}" Width="80"/>
                                <DataGridTextColumn Header="Найменування" Binding="{Binding Description}" Width="*"/>
                                <DataGridTextColumn Header="Кількість" Binding="{Binding Quantity}" Width="80"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                            <Button Style="{StaticResource AddButtonStyle}" Content="Додати" Command="{Binding AddSoftwareEquipmentCommand}" Width="100"/>
                            <Button Style="{StaticResource EditButtonStyle}" Content="Змінити" Command="{Binding EditSoftwareEquipmentCommand}" Width="100"/>
                            <Button Style="{StaticResource DeleteButtonStyle}" Content="Видалити" Command="{Binding DelSoftwareEquipmentCommand}" Width="100"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Knopky diyi -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" >
            <Button Style="{StaticResource PrimaryButtonStyle}" Content="{Binding ButtonName}" Command="{Binding AddPropertyCommand}" Width="150"/>
            <Button Style="{StaticResource SecondaryButtonStyle}" Content="Повернутись назад" Command="{Binding HomeCommand}" Width="150"/>
        </StackPanel>
    </Grid>
</UserControl>
