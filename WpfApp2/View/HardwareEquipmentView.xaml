<UserControl x:Class="Vuzol.View.HardwareEquipmentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Vuzol.View"
             mc:Ignorable="d"  Background="White" d:Height="450" d:Width="800">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <ComboBox ItemsSource="{Binding SubPropertyFactoryNumberList}" 
       SelectedValue="{Binding SubPropertyFactoryNumber,UpdateSourceTrigger=PropertyChanged}"  
       Grid.Row="1" Grid.Column="0" Width="400" Height="30"  />
        <Label  Grid.Column="1" Grid.Row="0" Content="Опис"/>
        <TextBox Name="Description" Grid.Column="1" Grid.Row="1"  
                 Text="{Binding Description,UpdateSourceTrigger=PropertyChanged}"/>
        <Label  Grid.Column="2" Grid.Row="0" Content="Кількість"/>
        <TextBox Name="Quantity" Grid.Column="2" Grid.Row="1"  
                 Text="{Binding Quantity,UpdateSourceTrigger=PropertyChanged}"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Center" Grid.ColumnSpan="3">
            <Button Content="{Binding ButtonName}" Command="{Binding ChangeHardwareEquipmentCommand}"  Width="200"/>
            <Button Content="Повернутись назад" Command="{Binding HomeCommand}"  Width="200"/>
        </StackPanel>
    </Grid>
</UserControl>
