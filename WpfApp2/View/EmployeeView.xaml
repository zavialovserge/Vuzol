<UserControl x:Class="Vuzol.View.EmployeeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Vuzol.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="#FAFAFA">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <DataGrid Name="employeeDataGrid" 
                      ItemsSource="{Binding EmployeesList}" 
                      SelectedItem="{Binding SelectedEmployee}" 
                      SelectedValue="{Binding SelectedEmployee}" 
                      IsReadOnly="True" 
                      AutoGenerateColumns="False"
                      Style="{StaticResource ModernDataGridStyle}"
                      ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                      CellStyle="{StaticResource DataGridCellStyle}"
                      RowStyle="{StaticResource DataGridRowStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Width="Auto" Header="Номер" Binding="{Binding Id}"/>
                    <DataGridTextColumn Width="Auto" Header="Ім'я" Binding="{Binding FirstName}"/>
                    <DataGridTextColumn Width="Auto" Header="Прізвище" Binding="{Binding LastName}"/>
                    <DataGridTextColumn Width="Auto" Header="По батькові" Binding="{Binding FatherName}"/>
                    <DataGridTextColumn Width="Auto" Header="Посада" Binding="{Binding Position}"/>
                    <DataGridTextColumn Width="Auto" Header="Звання" Binding="{Binding RankDescription}"/>
                    <DataGridTextColumn Width="Auto" Header="Підрозділ" Binding="{Binding UnitName}"/>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,10,0,10">
            <Button Content="Додати" Name="AddBtn" Command="{Binding AddEmployeeCommand}" Style="{StaticResource AddButtonStyle}"/>
            <Button Content="Коригувати" Name="EditBtn" Command="{Binding EditEmployeeyCommand}" Style="{StaticResource EditButtonStyle}"/>
            <Button Content="Видалити" Name="DelBtn" Command="{Binding DelEmployeeCommand}" Style="{StaticResource DeleteButtonStyle}"/>
            <Button Content="Повернутись назад" Name="HomeBtn" Command="{Binding HomeCommand}" Style="{StaticResource SecondaryButtonStyle}"/>
        </StackPanel>
    </Grid>
</UserControl>
