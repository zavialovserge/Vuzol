<UserControl x:Class="Vuzol.View.UnitView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Vuzol.View"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="800" Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <DataGrid HorizontalAlignment="Left" 
                  Grid.Row="0" Name="employeeDataGrid" ItemsSource="{Binding UnitList}" 
                  SelectedItem="{Binding SelectedUnit}" SelectedValue="{Binding SelectedUnit}" 
                    IsReadOnly="True" AutoGenerateColumns="False">
            <DataGrid.Columns >
                <DataGridTextColumn Width="100"   Header="Номер" Binding="{Binding Id}" >
                    <DataGridTextColumn.HeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="HorizontalContentAlignment"
                Value="Center" />
                        </Style>
                    </DataGridTextColumn.HeaderStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Width="700" Header="Назва підрозділу" Binding="{Binding Name}">
                    <DataGridTextColumn.HeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="HorizontalContentAlignment"
                                    Value="Center" />
                        </Style>
                    </DataGridTextColumn.HeaderStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Content="Додати" Name="AddBtn" Command="{Binding AddUnitCommand}"  Width="200"/>
            <Button Content="Коригувати" Name="EditBtn" Command="{Binding EditUnitCommand}"  Width="200"/>
            <Button Content="Видалити" Name="DelBtn" Command="{Binding DelUnitCommand}"  Width="200"/>
            <Button Content="Повернутись назад" Name="HomeBtn" Command="{Binding HomeCommand}"  Width="200"/>
        </StackPanel>
    </Grid>
</UserControl>
